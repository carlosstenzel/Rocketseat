const { resolve } = require('path');

module.exports = {
  config: resolve(__dirnamem, 'src', 'config', 'database.js'),
  'models-path': resolve(__dirnamem, 'src', 'app', 'models'),
  'migration-path': resolve(__dirnamem, 'src', 'database', 'migrations'),
  'seeders-path': resolve(__dirnamem, 'src', 'database', 'seeds')
}